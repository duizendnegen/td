// See ARCHITECTURE.md §12
import { describe, it } from 'vitest';

describe('level and balance schemas', () => {
  it.todo('rejects a wave group referencing an undeclared spawn id');
  it.todo('rejects a wave group referencing an unknown enemy type');
  it.todo('rejects a level whose treasury is unreachable from a spawn');
  it.todo('converts float rates to integers at load');
});

// TODO(P4): implement
