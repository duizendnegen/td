// See ARCHITECTURE.md §12
import { describe, it } from 'vitest';

describe('placement', () => {
  it.todo('rejects a placement that seals every spawn from the treasury');
  it.todo('rejects a placement that strands a live enemy inside the maze');
  it.todo('rejects a placement whose footprint contains an enemy');
  it.todo('removal keeps the tile blocked for all 80 ticks');
});

// TODO(P2): implement
