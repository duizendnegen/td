// See ARCHITECTURE.md §12
import { describe, it } from 'vitest';

describe('theft round trip', () => {
  it.todo('enemy at the treasury grabs min(capacity, balance) and flips to returning');
  it.todo('carrier moves at 80% speed');
  it.todo('killing a carrier drops its sack on its tile');
  it.todo('an inbound enemy picking up gold flips to returning immediately');
  it.todo('gold escaping through a spawn is gone');
  it.todo('unclaimed sacks return to the treasury at end of wave');
});

// TODO(P2): implement
