// See ARCHITECTURE.md §12
import { describe, it } from 'vitest';

describe('flow fields', () => {
  it.todo('every walkable tile reaches the treasury on open terrain');
  it.todo('never points diagonally between two blocked tiles');
  it.todo('costs decrease monotonically toward the source');
  it.todo('returning field targets the nearest ACTIVE spawn');
});

// TODO(P1): implement
