// Pointer picking and command emission
// See ARCHITECTURE.md §9
//
// Responsibilities:
//   - Raycast against the ground plane to get a tile coordinate
//   - Ghost preview runs the REAL validation function via a dryRun flag
//   - Never writes sim state directly — emits commands only

// TODO(P1): not implemented
export {};
